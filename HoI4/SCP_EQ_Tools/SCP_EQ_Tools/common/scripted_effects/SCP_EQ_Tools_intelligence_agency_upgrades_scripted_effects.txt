SCP_EQ_Tools_intelligence_agency_upgrades_complete_all_upgrades = {
    if = {
        limit = {
            has_intelligence_agency = no
        }
        create_intelligence_agency = yes
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_economy_civilian }
        }
        upgrade_intelligence_agency = upgrade_economy_civilian
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_army_department }
        }
        upgrade_intelligence_agency = upgrade_army_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_naval_department }
        }
        upgrade_intelligence_agency = upgrade_naval_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_airforce_department }
        }
        upgrade_intelligence_agency = upgrade_airforce_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_passive_defense }
        }
        upgrade_intelligence_agency = upgrade_passive_defense
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_anti_partisan }
        }
        upgrade_intelligence_agency = upgrade_anti_partisan
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_blueprint_stealing }
        }
        upgrade_intelligence_agency = upgrade_blueprint_stealing
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_portable_radios }
        }
        upgrade_intelligence_agency = upgrade_portable_radios
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_invisible_ink }
        }
        upgrade_intelligence_agency = upgrade_invisible_ink
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_plastic_explosives }
        }
        upgrade_intelligence_agency = upgrade_plastic_explosives
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_suicide_pills }
        }
        upgrade_intelligence_agency = upgrade_suicide_pills
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_training_centers }
        }
        upgrade_intelligence_agency = upgrade_training_centers
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_commando_training }
        }
        upgrade_intelligence_agency = upgrade_commando_training
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_interrogation_techniques }
        }
        upgrade_intelligence_agency = upgrade_interrogation_techniques
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_diplo_training }
        }
        upgrade_intelligence_agency = upgrade_diplo_training
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_psycho_warfare }
        }
        upgrade_intelligence_agency = upgrade_psycho_warfare
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_form_department }
        }
        upgrade_intelligence_agency = upgrade_form_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_decryption_boost }
        }
        upgrade_intelligence_agency = upgrade_decryption_boost
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_decryption_boost_2 }
        }
        upgrade_intelligence_agency = upgrade_decryption_boost_2
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_crypto_strength }
        }
        upgrade_intelligence_agency = upgrade_crypto_strength
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_crypto_strength_2 }
        }
        upgrade_intelligence_agency = upgrade_crypto_strength_2
    }
}

SCP_EQ_Tools_intelligence_agency_upgrades_complete_all_upgrades_EAW = {
    if = {
        limit = {
            has_intelligence_agency = no
        }
        create_intelligence_agency = yes
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_economy_civilian }
        }
        upgrade_intelligence_agency = upgrade_economy_civilian
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_army_department }
        }
        upgrade_intelligence_agency = upgrade_army_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_naval_department }
        }
        upgrade_intelligence_agency = upgrade_naval_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_airforce_department }
        }
        upgrade_intelligence_agency = upgrade_airforce_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_passive_defense }
        }
        upgrade_intelligence_agency = upgrade_passive_defense
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_anti_partisan }
        }
        upgrade_intelligence_agency = upgrade_anti_partisan
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_blueprint_stealing }
        }
        upgrade_intelligence_agency = upgrade_blueprint_stealing
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_portable_radios }
        }
        upgrade_intelligence_agency = upgrade_portable_radios
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_invisible_ink }
        }
        upgrade_intelligence_agency = upgrade_invisible_ink
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_plastic_explosives }
        }
        upgrade_intelligence_agency = upgrade_plastic_explosives
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_suicide_pills }
        }
        upgrade_intelligence_agency = upgrade_suicide_pills
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_training_centers }
        }
        upgrade_intelligence_agency = upgrade_training_centers
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_commando_training }
        }
        upgrade_intelligence_agency = upgrade_commando_training
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_interrogation_techniques }
        }
        upgrade_intelligence_agency = upgrade_interrogation_techniques
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_diplo_training }
        }
        upgrade_intelligence_agency = upgrade_diplo_training
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_psycho_warfare }
        }
        upgrade_intelligence_agency = upgrade_psycho_warfare
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_form_department }
        }
        upgrade_intelligence_agency = upgrade_form_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_decryption_boost }
        }
        upgrade_intelligence_agency = upgrade_decryption_boost
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_crypto_strength }
        }
        upgrade_intelligence_agency = upgrade_crypto_strength
    }
}

SCP_EQ_Tools_intelligence_agency_upgrades_complete_all_upgrades_TFR = {
    if = {
        limit = {
            has_intelligence_agency = no
        }
        create_intelligence_agency = yes
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_economy_civilian }
        }
        upgrade_intelligence_agency = upgrade_economy_civilian
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_army_department }
        }
        upgrade_intelligence_agency = upgrade_army_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_naval_department }
        }
        upgrade_intelligence_agency = upgrade_naval_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_airforce_department }
        }
        upgrade_intelligence_agency = upgrade_airforce_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_passive_defense }
        }
        upgrade_intelligence_agency = upgrade_passive_defense
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_anti_partisan }
        }
        upgrade_intelligence_agency = upgrade_anti_partisan
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_blueprint_stealing }
        }
        upgrade_intelligence_agency = upgrade_blueprint_stealing
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_portable_radios }
        }
        upgrade_intelligence_agency = upgrade_portable_radios
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_invisible_ink }
        }
        upgrade_intelligence_agency = upgrade_invisible_ink
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_plastic_explosives }
        }
        upgrade_intelligence_agency = upgrade_plastic_explosives
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_suicide_pills }
        }
        upgrade_intelligence_agency = upgrade_suicide_pills
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_training_centers }
        }
        upgrade_intelligence_agency = upgrade_training_centers
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_commando_training }
        }
        upgrade_intelligence_agency = upgrade_commando_training
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_interrogation_techniques }
        }
        upgrade_intelligence_agency = upgrade_interrogation_techniques
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_diplo_training }
        }
        upgrade_intelligence_agency = upgrade_diplo_training
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_psycho_warfare }
        }
        upgrade_intelligence_agency = upgrade_psycho_warfare
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_form_department }
        }
        upgrade_intelligence_agency = upgrade_form_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_decryption_boost }
        }
        upgrade_intelligence_agency = upgrade_decryption_boost
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_decryption_boost_2 }
        }
        upgrade_intelligence_agency = upgrade_decryption_boost_2
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_crypto_strength }
        }
        upgrade_intelligence_agency = upgrade_crypto_strength
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_crypto_strength_2 }
        }
        upgrade_intelligence_agency = upgrade_crypto_strength_2
    }
}

SCP_EQ_Tools_intelligence_agency_upgrades_complete_all_upgrades_TNO = {
    if = {
        limit = {
            has_intelligence_agency = no
        }
        create_intelligence_agency = yes
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_economy_department }
        }
        upgrade_intelligence_agency = upgrade_economy_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_army_department }
        }
        upgrade_intelligence_agency = upgrade_army_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_naval_department }
        }
        upgrade_intelligence_agency = upgrade_naval_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_airforce_department }
        }
        upgrade_intelligence_agency = upgrade_airforce_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_passive_defense }
        }
        upgrade_intelligence_agency = upgrade_passive_defense
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_anti_guerrilla_activities }
        }
        upgrade_intelligence_agency = upgrade_anti_guerrilla_activities
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_industrial_espionage }
        }
        upgrade_intelligence_agency = upgrade_industrial_espionage
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_wiretapping }
        }
        upgrade_intelligence_agency = upgrade_wiretapping
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_agency_infiltration }
        }
        upgrade_intelligence_agency = upgrade_agency_infiltration
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_surveillance_aircraft }
        }
        upgrade_intelligence_agency = upgrade_surveillance_aircraft
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_computer_hacking }
        }
        upgrade_intelligence_agency = upgrade_computer_hacking
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_global_training_centers }
        }
        upgrade_intelligence_agency = upgrade_global_training_centers
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_special_forces_training }
        }
        upgrade_intelligence_agency = upgrade_special_forces_training
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_enhanced_interrogation_methods }
        }
        upgrade_intelligence_agency = upgrade_enhanced_interrogation_methods
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_diplomatic_training }
        }
        upgrade_intelligence_agency = upgrade_diplomatic_training
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_psychological_warfare }
        }
        upgrade_intelligence_agency = upgrade_psychological_warfare
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_modernize_department }
        }
        upgrade_intelligence_agency = upgrade_modernize_department
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_cryptographic_engineering }
        }
        upgrade_intelligence_agency = upgrade_cryptographic_engineering
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_government_developed_ciphers }
        }
        upgrade_intelligence_agency = upgrade_government_developed_ciphers
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_public_key_cryptography }
        }
        upgrade_intelligence_agency = upgrade_public_key_cryptography
    }
    while_loop_effect = {
        limit = {
            NOT = { has_done_agency_upgrade = upgrade_computerized_cracking }
        }
        upgrade_intelligence_agency = upgrade_computerized_cracking
    }
}

