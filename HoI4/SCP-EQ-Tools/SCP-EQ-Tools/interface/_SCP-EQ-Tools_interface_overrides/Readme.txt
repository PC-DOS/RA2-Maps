This directory contians GUI files for overriding. 

These files are not enabled by default, to avoid affecting the main game.

Copy them to "interface" directory of the target mod indicated by corresponding directory name (remember to backup the original file) if you need them.

These GUI files are tested under specified mod version, we don't guarantee the compatibility between different mod versions.